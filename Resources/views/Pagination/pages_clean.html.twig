{# default Sliding pagination control implementation #}

{% if pageCount > 1 %}
<div class="cmscore_pagination">
{#
    {% if first is defined and current != first %}
        <span class="cmscore_first">
            <a href="{{ path(route, query|merge({'page': first})) }}">&lt;&lt;</a>
        </span>
    {% endif %}
#}

    {% if previous is defined %}
        <span class="cmscore_previous">
            <a href="{{ path(route, query|merge({'page': previous})) }}">&lt; Previous page</a>
        </span>
    {% endif %}

    {% for page in pagesInRange %}
        {% if page != current %}
            <span class="cmscore_page">
                <a href="{{ path(route, query|merge({'page': page})) }}">{{ page }}</a>
            </span>
        {% else %}
            <span class="cmscore_page cmscore_current">{{ page }}</span>
        {% endif %}

    {% endfor %}

    {% if next is defined %}
        <span class="cmscore_next">
            <a href="{{ path(route, query|merge({'page': next})) }}">Next page &gt;</a>
        </span>
    {% endif %}

{#
    {% if last is defined and current != last %}
        <span class="cmscore_last">
            <a href="{{ path(route, query|merge({'page': last})) }}">&gt;&gt;</a>
        </span>
    {% endif %}
#}
</div>
{% endif %}
